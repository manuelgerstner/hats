# This is the main configuration file for the application.
# ~~~~~

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
# If you deploy your application to several instances be sure to use the same key!
application.secret="3s=@J0^NOKm^xOrT=1a<]C^_YI`i_<eVQbQH:wM[[J^OfA4^J<H5f[nNuS55oYO="

# The application languages
# ~~~~~
application.langs="en"

# Global object class
# ~~~~~
# Define the Global object class for this application.
# Default to Global in the root package.
# application.global=Global

# Router
# ~~~~~
# Define the Router object to use for this application.
# This router will be looked up first when the application is starting up,
# so make sure this is the entry point.
# Furthermore, it's assumed your route file is named properly.
# So for an application router like `my.application.Router`,
# you may need to define a router file `conf/my.application.routes`.
# Default to Routes in the root package (and conf/routes)
# application.router=my.application.Routes

# Database configuration
# ~~~~~
# You can declare as many datasources as you want.
# By convention, the default datasource is named `default`
#
db.default.driver=org.h2.Driver

# use this to put database file in Play's project directory, under /database/ with file prefix sixthinkinghats
db.default.url="jdbc:h2:mem:database/sixthinkinghats"
#db.default.url="jdbc:h2:mem:play;MODE=MYSQL"
db.default.logStatements=true
db.default.idleConnectionTestPeriod=1 minutes
db.default.connectionTestStatement="SELECT NOW()"
db.default.maxConnectionAge=2 second
db.default.idleMaxAge=5 second
db.default.connectionTimeout=10 second
db.default.idleTimeout=5 second

#db.default.maxConnectionAge=30 minutes

# db.default.user=sa
# db.default.password=""

# Evolutions
# ~~~~~
# You can disable evolutions if needed
# evolutionplugin=disabled

# Logger
# ~~~~~
# You can also configure logback (http://logback.qos.ch/),
# by providing an application-logger.xml file in the conf directory.

# Root logger:
logger.root=ERROR

# Logger used by the framework:
logger.play=INFO

# Logger provided to your application:
logger.application=DEBUG

# SMTP configuration
include "smtp.conf"

# play-easymail configuration
include "play-easymail.conf"
